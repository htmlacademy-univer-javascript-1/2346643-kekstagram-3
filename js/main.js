import {randInt} from './util'; // eslint-disable-line no-unused-vars
import {stringLengthChecker} from './util'; // eslint-disable-line no-unused-vars
import {photosDataGenerator} from './data'; // eslint-disable-line no-unused-vars

const photosNumber = 25;
const photosData = photosDataGenerator(photosNumber); // eslint-disable-line no-unused-vars
